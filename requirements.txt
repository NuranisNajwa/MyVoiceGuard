flask>=2.3
flask-cors>=4.0
numpy>=1.24,<2
scipy>=1.10
# Exact pins so librosa MFCC + Numba work on Linux (Render); avoids get_call_template crashes.
numba==0.59.1
llvmlite==0.42.0
librosa==0.10.1
soundfile>=0.12
pydub>=0.25
scikit-learn>=1.3,<1.9
joblib>=1.3
requests>=2.31
# YouTube needs EJS challenge solvers (bundled via [default]) + Deno in Docker PATH.
yt-dlp[default]>=2026.1.29
awsgi>=0.0.5
webrtcvad>=2.0.10
gunicorn>=21.2
